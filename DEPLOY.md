# 🚀 Деплой FitTracker на Railway

## Шаг 1: Загрузка кода на GitHub

### 1.1 Создай репозиторий
1. Зайди на **github.com**
2. Нажми **+** → **New repository**
3. Название: `fittracker-bot`
4. Оставь **Public** или **Private**
5. НЕ добавляй README (мы загрузим свой код)
6. Нажми **Create repository**

### 1.2 Загрузи код
Можно через веб-интерфейс GitHub:
1. На странице нового репозитория нажми **uploading an existing file**
2. Загрузи все файлы из папки fittracker

Или через Git (если установлен):
```bash
cd fittracker
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/fittracker-bot.git
git push -u origin main
```

---

## Шаг 2: Настройка Supabase

### 2.1 Создай таблицы
1. Открой свой проект в **Supabase**
2. Слева выбери **SQL Editor**
3. Нажми **+ New query**
4. Скопируй содержимое файла `database/schema.sql`
5. Нажми **Run** (или Ctrl+Enter)
6. Должно появиться "Success. No rows returned"

---

## Шаг 3: Деплой на Railway

### 3.1 Создай проект
1. Зайди на **railway.app**
2. Нажми **New Project**
3. Выбери **Deploy from GitHub repo**
4. Найди и выбери `fittracker-bot`
5. Railway автоматически начнёт деплой

### 3.2 Добавь переменные окружения
1. Открой свой проект в Railway
2. Нажми на сервис (карточка с кодом)
3. Перейди во вкладку **Variables**
4. Добавь следующие переменные:

```
TELEGRAM_BOT_TOKEN = 7806658208:AAHDfEDSeykI3qG7_7aRSe98Rw-ItoRIKdQ
SUPABASE_URL = https://mtcyypvggtokcxzqxqhz.supabase.co
SUPABASE_KEY = sb_publishable_9tRD3TCa81Al2nUS_vow_Q_veLDkGTv
OPENAI_API_KEY = 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
NODE_ENV = production
```

5. **ВАЖНО:** WEBAPP_URL добавим после получения домена

### 3.3 Получи домен
1. В Railway перейди во вкладку **Settings**
2. Найди раздел **Domains**
3. Нажми **Generate Domain**
4. Скопируй полученный URL (например: `fittracker-bot-production.up.railway.app`)

### 3.4 Добавь WEBAPP_URL
1. Вернись во вкладку **Variables**
2. Добавь:
```
WEBAPP_URL = https://fittracker-bot-production.up.railway.app
```
(замени на свой домен)

### 3.5 Перезапусти сервис
Railway автоматически перезапустится после добавления переменных.

---

## Шаг 4: Проверка

### 4.1 Проверь логи
1. В Railway перейди во вкладку **Deployments**
2. Нажми на последний деплой
3. Открой **View Logs**
4. Должно быть:
   - `🚀 Server running on port 3000`
   - `🔗 Webhook set to https://...`

### 4.2 Проверь бота
1. Открой Telegram
2. Найди своего бота (по имени которое давал @BotFather)
3. Нажми **Start** или отправь `/start`
4. Должно появиться приветствие с кнопками

---

## Возможные проблемы

### Бот не отвечает
- Проверь логи в Railway
- Убедись что все переменные добавлены
- Проверь что WEBAPP_URL начинается с `https://`

### Ошибка базы данных
- Убедись что выполнил SQL в Supabase
- Проверь что SUPABASE_URL и KEY правильные

### Mini App не открывается
- WEBAPP_URL должен совпадать с доменом Railway
- Домен должен быть HTTPS

---

## После успешного деплоя

1. Протестируй бота сам
2. Добавь тестового клиента
3. Создай тестовую тренировку
4. Проверь что Mini App открывается

Если что-то не работает — скинь мне скриншот ошибки или логов!
